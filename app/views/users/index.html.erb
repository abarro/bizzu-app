<h1>Todos os vendedores</h1>

<div>
	<%= link_to 'Novo Vendedor(a)', new_user_path %>
	<ul>
		<% @users.each do |user| %>
		<li>
			<%= user.name %><br>
			<%= user.cpf %><br>
			<%= user.sales %><br>
			<%= link_to 'Ver', user_path(user) %> ou <%= link_to 'Editar', edit_user_path(user) %>
		</li>
		<% end %>
	</ul>

	<%= link_to 'Página Inicial', root_path %>

</div>